const DEFAULT_STATE = {
    products: [],
};

export const productReducer = (state = DEFAULT_STATE, action) => {
    switch (action.type) {
        case "SET_PRODUCTS":
        default:
            return state
    }
}