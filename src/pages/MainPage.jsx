import React from 'react';
import Layout from '../component/Layout';
import MainMenu from '../component/TransactionList';

const MainPage = () => {
  return (
      <Layout>
        <MainMenu />
      </Layout>
  )
};

export default MainPage;